# video-transcriber 依赖
# 核心链路纯 Python 标准库即可运行（云端 ASR 模式）：
#   - B 站下载：urllib（无需 requests）
#   - 云端 ASR：urllib 调用 SiliconFlow API（仅需 SILICONFLOW_API_KEY）
#
# 本地 ASR 模式才需要下面这些（funasr 会自动带入 torch 依赖）：
funasr>=1.2.6
modelscope>=1.9.5

# 控制台编码（Windows 路径含中文时有用）
python-dotenv>=1.0.0

# ffmpeg 是系统工具，需独立安装（Windows: winget install ffmpeg）
# 可选：yt-dlp（B 站直连失败时的兜底下载器，winget install yt-dlp）
